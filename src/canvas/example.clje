(ns canvas.example
  (:require [canvas.core :as c]))

(defn setup
  []
  nil)

(defn draw []
  (c/stroke (rand-int 255))
  (c/stroke-weight (rand-int 10))
  (c/fill (rand-int 255))

  (let [radius (rand-int 100)
        x      (rand-int (c/width))
        y      (rand-int (c/height))]
    (c/circle x y radius)))

(c/defsketch example
  :title "Oh so many circles"
  :size [500 500]
  :setup setup
  :draw draw
  :bgcolor [200])
